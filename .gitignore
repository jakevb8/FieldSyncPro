*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Android
app/build/
build/

# Kotlin
*.class

# Gradle wrapper cache only — the jar must be committed so CI can run
.gradle/wrapper/dists/

# Test results
app/build/reports/
app/build/test-results/

# Firebase — never commit, stored as GitHub secret GOOGLE_SERVICES_JSON
app/google-services.json
